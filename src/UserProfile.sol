// SPDX-License-Identifier: SEE LICENSE IN LICENSE
pragma solidity ^0.8.27;

/**
 * @title UserProfile
 * @dev DEPRECATED. Profile management moved off-chain to MongoDB.
 * Referral logic moved to ReferralRewards.sol.
 */
contract UserProfile {
    // This contract is no longer in use.
    // All profile data is now managed via the Circlepot API and stored in MongoDB.
    // Authentication is handled by Dynamic XYZ.
    // Referral logic is handled by ReferralRewards.sol.
}
